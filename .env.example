# Required for production launch. Set in Vercel → Project → Settings → Environment Variables.
# Applies to /api/unlock on the 2027 landing gate. Do not commit the real value here.
DEMO_PASSWORD=

# Local dev fallback when DEMO_PASSWORD is unset: demo-unlock.config.js (remove before public launch).
